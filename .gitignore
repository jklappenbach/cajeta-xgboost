# Build output
/build/
/dist/
*.cja
*.o
*.obj

# Generated / working
/.cajeta/
tmp/
*.tmp

# Per-target @Kernel access manifests, rewritten beside the extracted archive on
# every run of the suite or the tour. Generated, never authored.
/deps/

# Golden fixtures are committed under tools/fixtures/ — do NOT ignore them.
__pycache__/

# GPU reciprocal probe output (build input, never committed — reconstructed on CPU
# from the compact SFU seed; see tools/fdividef/README.md)
tools/fdividef/rcp_mantissa.npy
tools/fdividef/probe_rcp

# expf probe output (build input, never committed)
tools/expf/expf_sweep.npy
tools/expf/probe_expf
tools/expf/ex2_table.npy
tools/expf/model_libdevice
